overwrite: true
schema: 'api/**/*.graphql'
generates:
  api/graphql.d.ts:
    config:
      useIndexSignature: true
      mapperTypeSuffix: Model
      mappers:
        User: '@prisma/client/index.d#User'
    plugins:
      - typescript
      - typescript-resolvers
