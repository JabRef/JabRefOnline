extend type Query {
  """
  Get group by id.
  """
  group(id: ID!): Group
}

type Group {
  id: ID!
  name: String!
  children: [Group!]!

  color: String
  description: String
  icon: String

  documents: [Document!]!
}
